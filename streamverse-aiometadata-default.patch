diff --git a/app/src/main/java/com/nuvio/tv/data/local/AddonPreferences.kt b/app/src/main/java/com/nuvio/tv/data/local/AddonPreferences.kt
index ab3fd84..f808a6e 100644
--- a/app/src/main/java/com/nuvio/tv/data/local/AddonPreferences.kt
+++ b/app/src/main/java/com/nuvio/tv/data/local/AddonPreferences.kt
@@ -26,6 +26,8 @@ class AddonPreferences @Inject constructor(
 ) {
     companion object {
         private const val FEATURE = "addon_preferences"
+        private const val STREAMVERSE_METADATA_BASE_URL =
+            "https://aiometadata.elfhosted.com/stremio/7e1b6e37-b28d-4ecb-ab15-206d7f44d69f"
     }
 
     private fun effectiveProfileId(): Int {
@@ -50,6 +52,7 @@ class AddonPreferences @Inject constructor(
     private val userSetNamesKey = stringPreferencesKey("addon_user_set_names")
     private val addonEnabledStatesKey = stringPreferencesKey("installed_addon_enabled_states")
     private val streamVerseBootstrapV1Key = booleanPreferencesKey("streamverse_addon_bootstrap_v1")
+    private val streamVerseBootstrapV2Key = booleanPreferencesKey("streamverse_addon_bootstrap_v2")
     private val manifestSuffix = "/manifest.json"
 
     private fun canonicalizeUrl(url: String): String {
@@ -104,18 +107,30 @@ class AddonPreferences @Inject constructor(
     suspend fun ensureStreamVerseBootstrapAddons() {
         val ds = store()
         ds.edit { preferences ->
-            if (preferences[streamVerseBootstrapV1Key] == true) return@edit
-
-            val current = getCurrentList(preferences)
-            if (current.isEmpty()) {
+            var current = getCurrentList(preferences)
+            if (preferences[streamVerseBootstrapV1Key] != true && current.isEmpty()) {
                 val defaults = getDefaultAddons().toList()
                 preferences[orderedUrlsKey] = gson.toJson(defaults)
                 preferences[addonEnabledStatesKey] = gson.toJson(
                     defaults.associateWith { true }
                 )
                 preferences.remove(legacyUrlsKey)
+                current = defaults
             }
             preferences[streamVerseBootstrapV1Key] = true
+
+            if (preferences[streamVerseBootstrapV2Key] != true) {
+                val metadataUrl = canonicalizeUrl(STREAMVERSE_METADATA_BASE_URL)
+                if (current.none { canonicalizeUrl(it).equals(metadataUrl, ignoreCase = true) }) {
+                    current = listOf(metadataUrl) + current
+                    preferences[orderedUrlsKey] = gson.toJson(current)
+                }
+                val enabledStates = getCurrentEnabledStates(preferences).toMutableMap()
+                enabledStates[metadataUrl] = true
+                preferences[addonEnabledStatesKey] = gson.toJson(enabledStates)
+                preferences.remove(legacyUrlsKey)
+                preferences[streamVerseBootstrapV2Key] = true
+            }
         }
     }
 
@@ -246,6 +261,7 @@ class AddonPreferences @Inject constructor(
     }
 
     private fun getDefaultAddons(): Set<String> = setOf(
+        STREAMVERSE_METADATA_BASE_URL,
         "https://v3-cinemeta.strem.io",
         "https://opensubtitles-v3.strem.io"
     )

diff --git a/app/src/main/java/com/nuvio/tv/ui/screens/settings/EssentialPlaybackSettingsContent.kt b/app/src/main/java/com/nuvio/tv/ui/screens/settings/EssentialPlaybackSettingsContent.kt
--- a/app/src/main/java/com/nuvio/tv/ui/screens/settings/EssentialPlaybackSettingsContent.kt
+++ b/app/src/main/java/com/nuvio/tv/ui/screens/settings/EssentialPlaybackSettingsContent.kt
@@ -19,6 +19,7 @@ import androidx.hilt.navigation.compose.hiltViewModel
 import androidx.lifecycle.compose.collectAsStateWithLifecycle
 import com.nuvio.tv.R
 import com.nuvio.tv.data.local.AudioLanguageOption
+import com.nuvio.tv.data.local.PlayerPreference
 import com.nuvio.tv.data.local.StreamAutoPlayMode
 import com.nuvio.tv.ui.components.P2pConsentDialog
 import kotlinx.coroutines.launch
@@ -48,6 +49,30 @@ fun EssentialPlaybackSettingsContent(
             }
             item(key = "playback_basics") {
                 SettingsGroupCard(modifier = Modifier.fillMaxWidth(), title = stringResource(R.string.essential_playback_basics)) {
+                    SettingsActionRow(
+                        title = stringResource(R.string.playback_player),
+                        subtitle = stringResource(R.string.playback_section_player_desc),
+                        value = when (settings?.playerPreference) {
+                            PlayerPreference.INTERNAL -> stringResource(R.string.playback_player_internal)
+                            PlayerPreference.EXTERNAL -> stringResource(R.string.playback_player_external)
+                            PlayerPreference.ASK_EVERY_TIME -> stringResource(R.string.playback_player_ask)
+                            null -> ""
+                        },
+                        trailingIcon = Icons.Default.PlayArrow,
+                        onClick = {
+                            val current = settings?.playerPreference ?: PlayerPreference.INTERNAL
+                            val next = when (current) {
+                                PlayerPreference.INTERNAL -> PlayerPreference.EXTERNAL
+                                PlayerPreference.EXTERNAL -> PlayerPreference.ASK_EVERY_TIME
+                                PlayerPreference.ASK_EVERY_TIME -> PlayerPreference.INTERNAL
+                            }
+                            coroutineScope.launch { viewModel.setPlayerPreference(next) }
+                        },
+                        enabled = settings != null
+                    )
                     SettingsActionRow(
                         title = stringResource(R.string.essential_stream_selection),
                         subtitle = stringResource(R.string.essential_stream_selection_subtitle),
